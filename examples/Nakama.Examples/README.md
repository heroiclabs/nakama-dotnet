This project houses examples for use in our documentation. The examples only need to compile. They are not
intended to be run.

The `System.Console.Writeline()` calls must be replaced with `Debug.Log()` calls in Unity.